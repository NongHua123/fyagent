# Proposed Active-Spec Deletions

After implementation and review, remove the following active spec and its index entry:

```text
.trellis/spec/backend/wsl-macos-cross-build.md
```

Reason: the Linux/WSL-to-macOS local cross-build capability is intentionally retired. Git history preserves the former contract; it must not remain in active context injection.
