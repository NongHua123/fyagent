# Modernize CI and Release workflows — Implementation Plan

1. reconcile merged upstream workflows
2. pin runner labels and Action SHAs
3. build CI matrix and aggregate gate
4. implement strict toolchain/workflow contract checks
5. split platform Release jobs and Linux containers
6. implement eligibility, manifest, signing, provenance and publish gates
7. configure branch/tag/environment rules outside source

## Completion Evidence

Attach exact commands, relevant logs/artifacts, changed-file list, platform scope, unresolved limitations, and the owning spec updates. Run the parent GO/NO-GO evaluation before closing.
